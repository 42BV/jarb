== Java Repository Bridge (JaRB) ==

== Components ==
    * constraint-metadata (provides the constraints of an entity, via column metadata and constraint annotation inspection)
    * constraint-validation (@DatabaseConstrained annotation and validator)
    * constraint-violations (translates JDBC driver exceptions into constrain violation exceptions)
    * migrations (migrating datasource, providing atleast Liquibase integration)
    * populator (org.springframework.jdbc.datasource.init support, skipable and compound populator)
    * populator-excel (excel driven database populator)
    * utils (common utilities, available for any other component)